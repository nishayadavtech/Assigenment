# Preproute Frontend (Vite + React + TypeScript)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:5173 and login with vedant-admin / vedant123 (backend must run on http://localhost:4000)
